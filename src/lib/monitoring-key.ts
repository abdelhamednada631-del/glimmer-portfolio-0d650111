/**
 * HeronSignal public key (safe to commit — it is a publishable key).
 *
 * Paste the key from Dashboard → Install → Web SDK between the quotes, or set
 * `VITE_HERONSIGNAL_PUBLIC_KEY` in the environment. While it is empty the
 * tracker is never loaded, so the site ships zero monitoring bytes.
 */
export const HERONSIGNAL_PUBLIC_KEY = "";
